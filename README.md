# Han Fang Academic Homepage

This repository contains Han Fang's English academic homepage as a Jekyll site.

## Local Preview

Install Ruby and Bundler, then run:

```sh
bundle install
bundle exec jekyll serve
```

The site uses `baseurl: /fhinfo` for GitHub Pages project-site deployment.
